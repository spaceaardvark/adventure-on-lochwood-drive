{
  "name": "adventure-on-lochwood-drive",
  "version": "1.0.0",
  "description": "",
  "main": "dist/game.js",
  "scripts": {
    "build": "coffee --compile --bare --output dist/ src/",
    "watch": "coffee --watch --compile --bare --output dist/ src/",
    "serve": "http-server dist",
    "publish": "cd dist && surge && cd ..",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "spaceaardvark",
  "license": "MIT",
  "devDependencies": {
    "coffeescript": "^2.5.1"
  },
  "dependencies": {}
}
